APP_ENV=local
APP_DEBUG=true

DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=cdm_ss_manager
DB_USERNAME=root
DB_PASSWORD=

# Default passwords for admin/import flows (leave empty in production)
DEFAULT_USER_PASSWORD=
ADMIN_DEFAULT_PASSWORD=
IMPORT_DEFAULT_PASSWORD=

# Mail (optional — configure if email features are enabled)
MAIL_HOST=
MAIL_PORT=465
MAIL_FROM=
MAIL_FROM_NAME="CDM Sunday School"
MAIL_USER=
MAIL_PASS=
MAIL_ENCRYPTION=ssl

# Session hardening
SESSION_SAMESITE=Lax
